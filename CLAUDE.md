# Key Leak Guard — Claude Notes

This repo was generated by Bert's Bot Factory from the generic bot template.

Primary docs:
- `Spec.md` / `spec.md` — detailed product and implementation spec
- `acceptance-tests.md` — measurable soak criteria
- `bin/test-completion.sh` — stage-6 acceptance runner skeleton that future build tickets must wire to real fixtures

When implementing tickets, preserve the security constraint: never store or log raw secrets. Use HMAC fingerprints and redacted excerpts only.
